# AWR-SC-007: restricted material and large public log

This package retains the actual isolated r2 Codex production chain, two authored business changes, distinct independent review and original-client final delivery. Four actual business inputs are bound to one native client; independent business review and the final read-only check are different luna_worker participants. No restricted original was read.

The final four-file ZIP contains the public report, delivery note and two public attachments. Both public-report citations resolve inside the archive. The delivery note has ten project-local citations, of which seven are explicitly excluded log or internal audit references; the ZIP is not claimed to include those audit sources. Unknown reporting dates, metric definitions, causes, impacts and final dispositions remain explicit.

The exact 3,145,837-byte generated public log is omitted from Git without changing its original seals. See omitted-materials.json and control/reproduce-large-public-log.py for byte-identical reproduction and hashes. Full private native context, raw streams and runtime databases remain local. Bounded actual command receipts and source/artifact hashes are public.

Artifact packaging does not grant E4 completion. A later, separate acceptance record must bind the independently pushed artifact commit and exact verified remote SHA.
