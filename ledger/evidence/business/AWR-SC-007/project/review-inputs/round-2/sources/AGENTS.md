# Working on this business project

This directory is an independent business project with its own Git repository, source manifest and work ledger. Treat the current project files as the only business authority. Higher-priority host instructions still apply.

Use the installed AWR executable at `/Users/mac/Documents/originone/agent-work-running/target/release/awr` for actual work, sessions, claims, work branches, checkpoints, handoffs and evidence. Start every shell command with `rtk`, pass this project's absolute path where required, and prefer JSON receipts. Do not write the AWR database directly or fabricate runtime state, tool calls, review or delivery evidence.

The only allowed technical references outside this project are:

- `/Users/mac/Documents/originone/agent-work-running/README.md`
- `/Users/mac/Documents/originone/agent-work-running/docs/reference/cli-mcp-contract.md`
- `rtk /Users/mac/Documents/originone/agent-work-running/target/release/awr --help`
- The applicable `rtk /Users/mac/Documents/originone/agent-work-running/target/release/awr <subcommand> --help`

These references explain tool use; they are not business sources. Do not browse or broadly search the parent repository. Do not read the parent `tests/`, `crates/`, `docs/design/`, `ledger/` or `.local/` trees, other scenarios or runs, source fixture definitions, evaluator material, or any sibling `control/` directory. Do not read global memories, credentials, private configuration or unpublished future inputs. In particular, `../control/restricted-note.md`, `../control/operations.jsonl`, operator receipts and future-round prompts are outside the client boundary. A later source becomes usable only after the operator publishes it into this project.

Within this project, read the current `README.md`, `GOALS.md`, `PLAN.md`, `RULES.md`, `work-ledger.yaml`, `project.toml`, `DELIVERABLES.md`, and files currently present under `materials/`. You may also use state and receipts created by your own normal AWR and project work. Do not infer facts from filenames or from material that has not been published.

Begin from current project sources, choose ready work through actual AWR state, and preserve command receipts, checkpoints, source hashes and artifact revisions. When a large public log is later published under `materials/`, determine its actual size, record count, batch count and analysis method from that material yourself. Do not rely on operator-provided expected numbers, and do not copy the full log into a report.

Only public project materials may support business conclusions. Restricted originals and unpublished internal notes must not be read, imported, indexed, quoted or summarized. State missing facts and unverified causes as gaps. Do not launch subagents or other model clients; the operator coordinates later turns and independent review.

