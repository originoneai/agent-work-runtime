# Working on this business project

This directory is an independent project with its own Git repository, source manifest and work ledger. Use this project's `project.toml`, `GOALS.md`, `PLAN.md`, `RULES.md` and `work-ledger.yaml` as its business sources. Parent repository development ledgers refer to a different project. Higher-priority host instructions still apply.

Use AWR to manage the work and preserve recoverable progress. Its executable is `/Users/mac/Documents/originone/agent-work-running/target/release/awr`. Every shell command starts with `rtk`; `rtk proxy` preserves the command's output. Pass this project's absolute path with `--project` and use `--json` for receipts. Inspect command help when needed. If `.awr/` is absent, preview the existing manifest with `init --manifest project.toml`, inspect it, then initialize with `--accept`; this source mapping has been authorized for this project.

Read current AWR status and selectable work. Start or resume your actual work session with a claim before mutations. Use bootstrap for orientation and complete execution context for business work; preserve missing facts and provenance. Choose work from the sources, use the actual observed revision, and progress or complete source work through AWR. Source completion requires real artifacts and matching evidence. Save actual CLI receipts in `.work-receipts/`, checkpoint meaningful progress and unresolved work at each handoff, and inspect existing receipts before retrying an interrupted mutation. Do not directly write the AWR database or fabricate evidence or review.

Write business artifacts in `deliverables/` using `DELIVERABLES.md` for naming and ownership. Preserve source citations and explain what remains uncertain. Independent review is performed by a different participant; you may prepare work for review but must not sign it as independently reviewed. You are not alone in the codebase: preserve other participants' edits. Do not launch subagents or other model clients; the operator coordinates the independent participants.

Stay in this business project. The sibling `control/` directory, parent scenario specifications, later prompts and other projects are operator material, outside your work scope. Normal CLI help and the AWR integration reference at `/Users/mac/Documents/originone/agent-work-running/docs/integrations/codex.md` are available for operating the installed tool. Do not inspect user credential files or change global client settings or hooks.
