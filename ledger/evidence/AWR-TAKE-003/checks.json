{
  "checked_at": "2026-09-09T06:13:40.555419+00:00",
  "commands": [
    "cargo test --offline --locked -p awr-cli --test execution_registry --test client_continuity",
    "cargo test --offline --locked -p awr-cli --test execution_registry concurrent_registration (3 repetitions)"
  ],
  "result": "11 passed, 2 child fixture entrypoints ignored in the outer runner and explicitly invoked by the tests; 3/3 concurrency repetitions passed",
  "coverage": [
    "register before dispatch; command survives caller and session end",
    "stdout/stderr, terminal receipt, exit code and spawn failure",
    "project-wide key idempotence across sessions and concurrent processes",
    "changed-intent collision and protected execution event rejection",
    "external reference retains unverified state",
    "client continuity regression"
  ],
  "scope": "Owned local subprocess and CLI checks. No native client activation, E4 or new package release."
}
